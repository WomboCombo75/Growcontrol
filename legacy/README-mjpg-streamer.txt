The mjpg-streamer tree is not stored in git (nested clone, large build artifacts).
For webcam experiments, clone upstream into this folder, e.g.:
  git clone --depth 1 https://github.com/jacksonliam/mjpg-streamer.git mjpg-streamer
